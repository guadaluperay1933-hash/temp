# 果然鲜仓储四表系统

新疆果然鲜仓储有限公司的四本联动台账：水果进销存、物料与周转物、财务账套、综合查询对账单。
四个文件靠跨文件公式串在一起，**必须放在同一个文件夹里、文件名不能改**，打开时选「更新链接」。

## 文件

| 文件 | 说明 |
|---|---|
| `01_水果进销存台账模板.xlsx` | 原料入库 → 原料出库 → 成品出库 → 库存与吨位 |
| `02_物料与周转物台账模板.xlsx` | 包装物料 / 公司周转筐 / 客户自备筐 / 次果筐与托盘 |
| `03_财务账套与报表模板.xlsx` | 小企业会计准则账套，含资金日记账、凭证、报表 |
| `04_综合查询对账单模板.xlsx` | 各类对账单集中在这一本 |
| `参考/原0X_….xlsx` | 你交给我的四个原件，存档 |
| `修改脚本/` | 把原件改造成成品的脚本，见文末 |

---

# 这一轮改了什么（第八轮）

> 你提的五条，逐条对应下面五节。金额口径按你选的「金额只算货款，运费单独列」来做。

## ① 01 表：购买方筛选、销售金额、销售汇总

**购买方那一列的下拉指错了。** 你在【基础资料】新插了 D 列「购买方」，
Excel 把大部分下拉都自动往后挪对了，唯独【成品出库明细】的 F/G/H 三列原来共用
**同一个下拉区**（都指向 C 货主/客户），插列以后三列还是一起指着 C ——
所以 H「购买方」点开下拉，出来的是货主名单。现在 F/G 取「货主/客户」、H 取「购买方」。

**顺手把这个坑一次性堵死**：01、02、03 三本里**所有**取自基础资料/科目表的下拉，
全部改成「按表头名字找列、有多少条就多长」的动态区域（定义名称，如 `购买方表`）：

```
=OFFSET(基础资料!$A$4, 0, MATCH("购买方",基础资料!$A$3:$P$3,0)-1,
        MAX(1, COUNTA(INDEX(基础资料!$A$4:$P$103, 0, MATCH("购买方",基础资料!$A$3:$P$3,0)))), 1)
```

意思是「去表头那一行找叫『购买方』的列，从第 4 行起、有几条就取几条」。
以后**再插列、再删列、往下加内容，下拉都自动跟着走**，不会再错位，也不用回来改公式。
三本一共建了 **42 个**这样的动态下拉区，覆盖 **57 个**下拉。
（【基础资料】的「购买方」列原来是空的，我按你已经录的数据补了 4 个：张三、李四、王二、赵一。）

**销售金额口径**（按你选的那条）：

| 列 | 改之前 | 改之后 |
|---|---|---|
| 销售金额（应收） | 数量×销售单价 **＋自结运费＋代发运费** | **数量×销售单价**（只算货款） |
| 采购金额（应付） | 数量×采购单价 **＋采购代发运费** | **数量×采购单价**（只算货款） |
| 新增 AI 应收合计 | — | 销售金额 ＋ 自结运费 ＋ 代发运费 |
| 新增 AJ 应付合计 | — | 采购金额 ＋ 采购代发运费 |

你录的那四笔，现在是这样：

| 货主 | 购买方 | 数量 | 采购单价 | 采购代发运费 | **采购金额** | **应付合计** | 销售单价 | 自结运费 | 代发运费 | **销售金额** | **应收合计** |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 王照雪 | 张三 | 1 | 30 | 10 | 30 | **40** | 40 | | | 40 | **40** |
| 麦麦提 | 李四 | 2 | 30 | | 60 | **60** | 40 | | 20 | 80 | **100** |
| 热合曼 | 王二 | 3 | 30 | | 90 | **90** | 40 | 10 | | 120 | **130** |
| 王灵云 | 赵一 | 4 | 30 | 12 | 120 | **132** | 40 | | | 160 | **160** |

**「销售汇总里全都是未收」的真凶：一个错别字。**
【果然鲜销售汇总】的「已收金额」原来比的是 `"已收清"`，
可【基础资料】收款状态下拉里写的是 **「已收讫」**（讫，不是清）——
一个字对不上，已收金额永远是 0，于是四笔全算未收。现在：

| 购买方 | 应收合计 | 已收金额 | 未收金额 |
|---|--:|--:|--:|
| 张三 | 40 | 40 | 0 |
| 李四 | 100 | 100 | 0 |
| 王二 | 130 | 0 | **130** |
| 赵一 | 160 | 160 | 0 |

正好就是你说的「只有 130 是未收款的」。

顺带：【果然鲜销售汇总】的购买方名单原来把几百个普通出库客户（电商/主播、王照雪…）
也排进来，每行全是 0；现在只列真有果然鲜销售的那几位。

## ② 02 表：次果筐汇总退回四列改红色负数

【次果筐汇总】的「退回-大托盘 / 公司一次性筐 / 公司杂周转筐 / 公司新周转筐」四列
显示成正数，旁边的「退回-公司红周转筐」「退回合计」却是红色负数 —— 同一排数字两种样子。
现在四列统一成 `[红]-#,##0`。

**只改显示格式，数值一个没动**，所以「退回合计＝各列相加」「未回合计＝出库−退回」照样对。
王照雪那一行：出库 112、退回 1+10+11+12+13＝47、未回 65，对得上。

## ③ 03 表：资金账户两列打架、区域太短、有的不跳科目

三个毛病其实是一串的：

**（1）「资金账户有 2 列一样的」。**【科目表】里 K 列和 S 列都叫「资金账户」：
你往 K 列加了 4 个新账户（工行-姜诗林、建行-张涛、慢慢微信、果然鲜微信），
可【资金日记账】的下拉挂的是 **S 列**，而且区域写死在 `$S$4:$S$9` —— 只有 6 个。
所以「新增无效」。
现在**只留 K 列一份**，右边 T 列「对应会计科目」跟 K 列**一行对一行**
（就像 M 费用项目 ↔ U 费用对应科目那样），S 列清空并标注「已并入 K 列」。
四个新账户的会计科目我按名字填好了：工行/建行→银行存款，两个微信→其他货币资金。

**（2）「设置的区域太短」。**【资金日记账】的资金账户、业务类型、费用项目三个下拉，
连同【自动规则】【成本费用登记表】等处，全部换成上面那种动态区域。
现在科目表里加一行，日记账立刻就能选到。

**（3）「有的不会自动跳转科目」。** 两个原因：

- 【科目表】的「费用项目」有 71 个，可右边「费用对应科目」只填到第 48 个 ——
  后面 23 个（医药费、赔付出库、借款、车充电、水果销售、还款、代办费、转入微信、
  车辆使用费、包装物料款、还信用卡、滴滴打车、个人消费、儿子消费、婆婆消费、杨洋消费、
  鱼消费、商店、超市、快递费、蛋糕、住宿费、劳保）没有对应科目。
  `INDEX` 取到空格子返回的是**数字 0** 而不是空文本，所以 `IFERROR` 兜不住，
  对应科目那一格就显示 0。**23 个已全部补上**，公式也加了兜底
  （取不到就走「管理费用/其他应收款/其他应付款」，不会再出现 0）。
- 【自动规则】只有 23 条业务类型，【科目表】里却有 43 个。缺的 24 条已按口径补齐
  （周转费、运费、其他服务费、押金收取/退还、装卸费支出、电费、水费、业务招待费、差旅费、
  车辆使用费、办公用品、财务、利息、租库定金、账户互转、家用、个人消费/收入、国外、
  永城财务…），规则表从 40 行放宽到 143 行。

你已经录的 **125 行资金日记账，现在每一行的「对应科目」「账户对应科目」都能取到**，一个 0 都没有。

**跟着放宽的还有两张表**：【期初余额】右边的资金账户期初从 6 个放宽到 40 个，
且账户名自动跟着【科目表】K 列走；【资金与往来余额表】的资金账户结存从 6 行放宽到 14 行，
你那 10 个账户现在全都列得出来。

> ⚠️ 【资金与往来余额表】第 20 行「账面货币资金」那里显示 **✘ 差额 -236,075.00**，
> 这**不是这次改坏的**（原件就是这个数）。原因是【期初余额】左边 D/E 两列里，
> 库存现金 / 银行存款 / 其他货币资金的期初**没填**，而右边资金账户的期初填了 236,075。
> 补的办法：在【期初余额】D 列把库存现金 0、银行存款 62,246、其他货币资金 173,829 填上，
> 再在贷方找一个对应科目（实收资本或未分配利润）配平就行 —— 这是账务口径，得你们定。

## ④ 04 表：公司购买果品、果然鲜销售全对不上

**病根有两个，都在我上一轮留下的：**

**（1）整整错了一行。**【对接源_01水果】最后 6 列（购买方 / 销售单价 / 自结运费 /
代发运费 / 采购代发运费 / 销售类型）写的是 `[1]对账明细接口!$X5`，
而同一行其它列写的是 `$B$4` —— 第 4 行去取源表第 5 行。
**购买方一错位，按购买方筛出来的明细自然全是别人的。** 1,200 行 × 6 列已全部回正。

**（2）【_自动清单】里几列互相踩。** 上一轮加「购买方」名单时，直接把 AG/AH/AI 三列
盖掉了，可那三列原本是【物料与筐子销售对账单】的取数序号 —— 两张对账单抢同一片格子。
更糟的是【公司购买果品对账单】的序号列 AK 写成了「累计的累计」（`=N(AK上一行)+AJ本行`，
而 AJ 本身已经是累计），算出来是 6486 这种数，`MATCH` 一个都对不上，所以那张表全是乱的。
现在：AG/AH/AI **还原**给物料与筐子销售用；购买方名单挪到 **AN~AQ**；
公司购买果品另起 **AL/AM** 两列，口径就是你说的「公司真买下的那几笔」——
只认【成品出库明细】里填了采购单价/采购金额的行。

**（3）两张对账单按新口径重排。**

【果然鲜销售对账单】（筛购买方）明细列改成：
日期 / 票据单号 / 品名 / 等级 / 数量 / 单位 / 销售单价 / **销售金额（应收）** /
自结运费 / 代发运费 / **应收合计** / 收款状态 / 备注；
右边汇总也跟着拆成 销售金额、其中·自结运费、其中·代发运费、应收合计、已收金额、未收金额。
「已收清」同样改成 **「已收讫」**。筛「张三」出来：1 箱、货款 40、应收合计 40、已收 40、未收 0。

【公司购买果品对账单】（筛货主）明细列改成：
日期 / 票据单号 / 品名 / 等级 / 数量 / 单位 / 采购单价 / **采购金额（应付）** /
采购代发运费 / **应付合计** / 付款状态 / 备注。
**筛「王照雪」出来就是 1 条**：苹果 1 箱、采购单价 30、货款 30、代发运费 10、
**应付合计 40**、已付清 —— 也就是你说的「果然鲜买了王照雪的苹果 1 箱卖给张三」那一笔。
她另外那几百行成品出库是**她自己的货存在我们这儿**，不是我们买的，不会再混进来。

## ⑤ 顺手修掉的

- 【04 应收应付汇总】顶上那句「往来单位 0/250」永远显示 0 —— 它读的 `_自动清单!$A$1`
  那一格放的是说明文字，`N(文字)=0`。改成读真正的去重计数。
- 【01 对账明细接口】的「金额」列改成取**应收合计**（货款＋运费，才是客户真要付的钱），
  另补「销售货款」「应付合计」两列给 03/04 取数；
  01 的【收入结算汇总】【财务取数接口】里凡是「实际要收/要付多少钱」的地方一并改口径。
- 【01 果然鲜采购明细】底部「按货主汇总」原来只有 采购金额/已付/未付，
  现在拆成 采购代发运费 / 采购金额（应付）/ **应付合计** / 已付金额 / 未付金额。

---

# 这一轮验证做到什么程度

- 四本**括号与引号配对自检**：`工具/check_formula.py` 扫了 **761,565 条公式，坏 0 条**。
  （这一步必须做 —— openpyxl 存少一个右括号不会报错，Excel/WPS 打开会把那一格**悄悄变空白**。）
- 按 01→02→03→04 的依赖顺序整册重算，**公式报错 0 处**
  （《03》【使用说明】里那句「链接断了不会满屏 #REF!」是说明文字，不是错误）。
- **动态下拉区逐个取值验证**：往空白格里写 `=COUNTA(名字)` 和 `=INDEX(名字,1)`、
  `=INDEX(名字,COUNTA(名字))` 真算一遍，确认每个名字取到的条数与首尾项都对。
  例：`购买方表` 4 条（张三…赵一）、`资金账户表` 10 条（库存现金…果然鲜微信）、
  `费用项目表` 71 条（电费…劳保）。
- **125 行资金日记账逐行检查**：对应科目、账户对应科目没有一行取不到（原来会显示 0）。
- 跨文件缓存已按最新的 01/02 重写（03 刷新 11,913 格、04 刷新 58,380 格），
  打开 03/04 第一眼看到的就是新数。
- 四本都只选中【主页】一张表，不会再弹「选择了多个工作表」。


---

# 上一轮改了什么（存档）

> 上一轮你提的四条，逐条对应下面四节。另外顺手挖出并修掉了两个「现在看不出来、一填数就炸」的坑，写在第五节。

## ① 04 表对账单「成品出库数量」算错了

**病根**：《01》的【对账明细接口】C 列「往来单位」，在处理成品出库那一段时取的是
【成品出库明细】的 **G 列「客户/领用方」**，而不是 **F 列「存放人/货主」**。

水果对账单是出给**货主**看的（"你存在我这儿的货，进了多少、出了多少"），
可成品出库行却按"卖给谁"去匹配货主，于是 38 笔王照雪的成品出库只对上了 4 笔。

| | 改之前 | 改之后 |
|---|---:|---:|
| 王照雪 成品出库笔数 | 4 笔 | **38 笔** |
| 王照雪 成品出库数量 | 145 | **3,661** |

**顺带**：入库/出库/结存的单位是「筐」，成品出库的单位是「箱」，原来右上角汇总把两种单位
并排放着，看着像能相减。现在汇总项都带上了单位，成品出库还拆成「箱」和「筐等」两行，
表头说明里也写清了「结存数量＝入库筐数−出库筐数，跟成品出库箱数没有加减关系」。

## ② 成品出库明细加「购买方」，采购和销售各出一套明细汇总

**《01》新增一列**：【成品出库明细】在「客户/领用方」G 后面插了 **H 列「购买方」**。
公司（果然鲜）买下王照雪的苹果再卖给刘瑞，就是 F 填王照雪、H 填刘瑞。
只是代加工出库、公司没买断的，购买方留空即可。
（插列会让 H 以后的列整体右移一格，本册 5.8 万条引用这张表的公式已经跟着改完，
表内公式、数据校验、合并区、筛选区、列宽也全部跟着走。逐格比对过 9.3 万个格子，
除了我特意补的那句说明，没有一格的值发生变化。）

**《01》新增三张表**：

| 新表 | 干什么 |
|---|---|
| 【果然鲜采购明细】 | 把填了「采购单价/采购金额」的行挑出来逐笔列，底部按货主汇总：买了谁多少果、付了多少、还欠多少。采购代发运费单列一栏 |
| 【果然鲜销售汇总】 | 按购买方汇总：卖给谁、多少钱、自结运费、代发运费、收了多少、还欠多少 |
| 【库存等级接口】 | 给《03》跨文件取「某货主某等级还剩多少」用 |

**【果然鲜销售明细】两处改动**：F 列改成「购买方/客户」——填了购买方就显示购买方，
没填才退回客户/领用方；命中规则从「销售类型以果然鲜开头」放宽到
「果然鲜开头 **或** 填了销售单价/销售金额」，公司自购自销的那些行也能进来了。

**《04》新增【果然鲜销售对账单】**：按购买方出，逐笔列出品名、等级、数量、销售单价、
自结运费、代发运费、销售金额，底部合计金额 / 已收款 / 应收款。
（买方向的【公司购买果品对账单】原本就有，只是没人填过采购单价所以是 0 行，现在链路已经理顺。）

## ③ 03 表的客户下拉全是「水果销售/筐子销售」

这一条有 **三个** 病根，缺一个都修不好：

1. **取错了列**。《03》【_自动清单】的往来单位池子分四段，第二段（600 行）写的是
   `=IF(资金日记账!$F4="","",资金日记账!$F4)` —— 资金日记账 **E 列才是往来单位，F 列是业务类型**。
   而业务类型的下拉内容正是「仓储费、周转费、装卸费、**水果销售**、运费、**筐子销售**、物料销售…」，
   跟你看到的一字不差。同一张表里 AF 列「往来单位(有收入)」取的就是 `$E`，就差这一列。
2. **过滤词写错**。第一段（302 行）只在「项目＝客户应收」时才取值，可 01/02 接口表按客户出的行，
   项目名叫「成品销售款 / 公司购买果品款 / 筐子销售款 / 物料销售款」，"客户应收"只是
   【收入成本映射】里一个占位名，没有任何一行数据会等于它 —— 所以真客户一个都进不来。
3. **跨文件链接串了**。《03》里 `[1]` 指向的是 **02 物料册**、`[2]` 才是 01 水果册，
   可【对接源_01水果】全表 3,522 处写的都是 `[1]` —— 两张对接源读的是同一本 02，
   01 的客户往来数据从来没进过 03。

三处一起修完，下拉里现在是 **15 个真实客户**：聂新平、王照雪、伍元虎、马锦祥、麦麦提、
吐尔洪、热合曼、贾建国、王灵云、杨艳玲、荆建君、努尔敦、王浩、全修塑业、新顺发泡网。

另外在池子后面又接了一段，直接取本册【原料入库计价】按货主汇总那一段的客户名 ——
就算哪天跨文件链接断了，下拉里也始终有人。【应收应付汇总表】的取数条件同步放宽。

## ④ 库存总结余表 + 库存价值与欠款比对

**《01》新增【库存总结余】**：就是你要的那个直观数 —— 一个货主一个品种一行，等级全部合并：

| 货主 | 品种 | 入库数量 | 出库数量 | 结余数量 |
|---|---|---:|---:|---:|
| 聂新平 | 苹果 | 1,729 | 1,729 | 0 |
| 王照雪 | 苹果 | 1,670 | 1,442 | 228 |
| 马锦祥 | 香梨 | 1,900 | 0 | 1,900 |

最右边还有一句大白话备注：「聂新平 的苹果：入库 1,729，出库 1,729，结余 0」。
要看细到等级的，前面那张【库存结余】原样保留，没动。
（两张表按货主+品种合并后逐行比对过，**0 行差异**。）

**《03》新增【库存价值与欠款比对】**，正好是你说的那个用法：

- **下半部分**按客户＋等级列出结余数量，「单价」那一列是淡黄色手工格，你按等级填个心里价，
  库存价值＝结余数量×单价 自动出。
- **上半部分**按客户把三个数摆在一起：**库存价值 ／ 应收款余额 ／ 差额（库存价值−应收）**，
  再给一句建议 ——「货够抵账 · 可以先不收」或「货不够抵 · 该收钱了」，差额为负会标红。
  最右边一句话说人话：「王照雪 库里还压着 50.00 元的货，欠我们 20.00 元，差额 30.00 元」。

配套新增【对接源_01库存】跨文件取《01》的【库存等级接口】。

## ⑤ 顺带修掉的两个「一填数就炸」的坑

这两处现在看不出来，是因为 42 行成品出库的「采购单价」全是空的，整条链一路返回空串。
**只要有人第一次填采购单价，01/03/04 三本的应收应付会同时算错。**

| 列 | 表头说的 | 格子里实际是 | 后果 |
|---|---|---|---|
| 采购代发运费 | 手工录入（浅蓝底） | `=数量×采购单价` | 采购金额（应付）＝采购货款＋这一列＝**2 倍货款**，真运费永远进不去 |
| 销售单价 | 手工录入（浅蓝底） | `=采购那四列相加` | 根本没法填单价，销售金额跟着错 |

现在这两列已经清成真正的手工格，采购金额和销售金额的公式本来就是对的，清完自然就准了。

另外给【成品出库明细】加了一列自动的 **AK「实际购买方」**（填了购买方就是购买方，
没填就沿用客户/领用方），【财务取数接口】按客户算应收时改用这一列，不用每处再写一遍兜底。

---

# 验证做到什么程度

- 四个文件放在同一个文件夹里、按 01→02→03→04 的依赖顺序整册重算，**公式报错 0 处**
  （《03》【使用说明】里有一处 `#REF!` 是原文里那句「链接断了不会满屏 #REF!」的说明文字，不是错误）。
- 插列那一步逐格比对：改造前后 **93,093 个格子，除了我特意补的一句说明，值完全一致**。
- 【库存总结余】与【库存结余】按货主+品种合并后逐行比对，**0 行差异**。
- 跨文件链接的缓存值已经按最新的 01/02 重写过一遍 —— 打开 03/04 第一眼看到的就是新数，
  不用等你手动更新链接。

---

# 重建

```
bash 修改脚本/run_fix08.sh      # 本轮（第八轮）一键重建，输出到 /tmp/claude-0/deliver
```

上一轮的一键脚本（从最早的原件跑起）：

```
cd 修改脚本
python3 build_all.py
```

单步跑也行：

| 脚本 | 干什么 |
|---|---|
| `fix01_购买方.py` | 《01》成品出库明细插「购买方」列，全册引用跟着平移 |
| `fix02_接口.py` | 《01》对账明细接口改口径补列；清掉两个被写死的手工列；加「实际购买方」 |
| `fix03_新表.py` | 《01》加库存总结余 / 果然鲜采购明细 / 果然鲜销售汇总 / 库存等级接口 |
| `fix04_03表.py` | 《03》修跨文件索引、重建往来单位池、放宽款项名 |
| `fix06_03新表.py` | 《03》加对接源_01库存 / 库存价值与欠款比对 |
| `fix05_04表.py` | 《04》对接源补列、水果对账单加单位、加果然鲜销售对账单 |
| `repair_ext_links.py` | openpyxl 存盘会丢掉跨文件链接的关系，这个补回去（每次 openpyxl 存过都要跑） |
| `refresh_link_cache.py` | 把 03/04 里跨文件链接的缓存值按最新的 01/02 重写 |
| `xlsx_util.py` / `style01.py` | 整列插入与公式列号平移的工具、新表的样式常量 |
| `run_fix08.sh` | **本轮一键**：改四本 → 补链接关系 → 括号自检 → 工作组自检 → 依赖顺序重算 → 刷新跨文件缓存 |
| `fix08_01.py` | 《01》动态下拉、购买方下拉指对、金额只算货款＋应收/应付合计、已收讫错别字 |
| `fix08_02.py` | 《02》次果筐汇总退回四列改红色负数、动态下拉 |
| `fix08_03.py` | 《03》资金账户并成一列、费用对应科目补齐、自动规则补齐、期初与余额表放宽、动态下拉 |
| `fix08_04.py` | 《04》对接源行号回正、_自动清单列位归位、两张对账单按新口径重排 |

---

# 颜色约定

淡黄色＝手工录入，浅灰/浅绿＝公式自动算的。灰色列被覆盖不会报错，但汇总会静默算错。

---

## 打开时提示「无法更新的链接 / 选择了多个工作表」怎么办

**先说结论：点「确定」就行，数字是对的。**【03】【04】里所有跨文件取到的数都已经
存成了缓存值（03 有 3,497 个、04 有 5,365 个），不更新链接也照样显示。

**这一版已经修掉了「工作组」那一半。** 原因是存盘时**一张工作表都没被标成选中**
（正常应该正好有一张）。WPS 碰到这种状态会判定成「选择了多个工作表」，于是弹那个框，
而且把【数据 → 编辑链接】整个变灰、点都点不动。现在四本都改成开机停在【主页】，
编辑链接可以正常点开了。

**要让链接真的能更新，这三条得满足：**

1. **四个文件必须放在同一个文件夹里。** 链接是按文件名找的（`01_水果进销存台账模板.xlsx`
   这种相对路径），分开放在不同目录就找不到。
2. **文件名一个字都不能改。** 从聊天里下载时如果重名，浏览器会存成
   `01_水果进销存台账模板(1).xlsx`，那就对不上了 —— 把括号那段删掉。
3. **第一次先开 01 和 02，再开 03 和 04。** 源文件已经在内存里，链接会直接更新成功。

改完还想手工指一次：【数据 → 编辑链接 → 更改源】，分别指到 01 和 02。

> 顺带说一句：**不要用「另存为」把这四个文件再存一遍**。03/04 的跨文件链接和
> 01/02 的一些公式在别的软件里存一道就可能变形。正常编辑、Ctrl+S 保存没问题。
